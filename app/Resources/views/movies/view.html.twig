{% extends 'base.html.twig' %}
{% block title %}
    Docupet web application
{% endblock %}
{% block body %}
    <div class="panel panel-info">
        <div class="panel-heading">
            <h3>VIEW MOVIE
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ logout_path() }}" class="btn btn-danger pull-right" style="margin-left:10px;">Logout</a>
                {% endif %}
            </h3>
        </div>
    <div class="list-group">
        <p class="list-group-item" >{{ movie.title }}</p>
        <p class="list-group-item" >{{ movie.description }}</p>
        <p class="list-group-item" >{{ movie.category }}</p>
        <p class="list-group-item" >{{ movie.rating }}/10</p>
        <p class="list-group-item" >
            <a href="{{ absolute_url(asset('')) }}movies" class="btn btn-primary">Back</a>
        </p>
    </div>
</div>
{% endblock %}